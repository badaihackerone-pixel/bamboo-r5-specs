---
version: 2
plan:
  project-key: A5SPECS
  key: MAIN
  name: A5SPECS - Main Plan
stages:
  - Default Stage:
      jobs:
        - Default Job
Default Job:
  tasks: []
---
version: 2
plan:
  key: A5SPECS-MAIN
plan-permissions:
  - users:
      - a5attacker
    permissions:
      - view
      - edit
      - build
      - clone
      - admin
      - createplanbranch
